// Q - 1 )Below are nested “K” for loops. What is the time complexity?
let n= Number(prompt('Enter the limit'))
for(let i=0;i<n;i++)
for(let j=0;j<n;i++)
for(let m=0;m<n;i++)
// …
// …
// …
// K loops in total
// …
for(let z=0;z<n;i++)
console.log('Hello World');

//Answer.1. :- 
//The time complexity of above code is : O(n)

//Q-2) What is the time complexity of the below code?

let N= Number(prompt('Enter the limit'))
let M= Number(prompt('Enter the limit'))
let a = 0;
let b=0;
for(let i=0;i<N;i++){
a = a + 1
}
for(let j=0;j<M;j++) {
b = b + 1
}

//Answer.2. :- 
//The time complexity of above code is : O(n)

//Q -3) What is the time complexity of the below code?

let n1 = Number(prompt('Enter the limit'))

let i = 1;
while(i*2<=n1)
{
console.log('Time complexity')
i += 1;
}

//Answer.3. :- 
//The time complexity of above code is : O(n)



